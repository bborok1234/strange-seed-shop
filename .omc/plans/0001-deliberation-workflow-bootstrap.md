# Pointer — Plan 0001

This is a **harness adapter pointer**, not a plan. The canonical plan lives at:

→ [`docs/studio/plans/0001-deliberation-workflow-bootstrap.md`](../../docs/studio/plans/0001-deliberation-workflow-bootstrap.md)

Per `feedback_harness_neutral_source_of_truth` memory: game studio plans, specs, personas, and workflow docs live in repo-native paths (`docs/`, `reports/`). `.omc/` only holds runtime/cache state and pointers like this one. Do not duplicate plan content here.
