## 요약

#290은 “AI 네이티브 게임 운영사” 목표로 방향을 되돌려, Studio Harness v3의 실제 live runner 진입점과 24h 사용법을 추가합니다. #276의 fixture checker에 머물지 않고 `npm run studio:v3:runner -- --once --dry-run`으로 GitHub queue/PR/main 상태를 읽고 state/report/heartbeat를 남깁니다.

## Small win

- 이번 PR이 만든 가장 작은 승리: `npm run studio:v3:runner -- --once --dry-run`이 runnable하고, queue empty를 stop이 아닌 `production-game-intake-required`로 분류하는 checker가 생겼습니다.

## Plan-first evidence

- Plan artifact: `items/0147-studio-v3-24h-live-runner.md`
- Plan에서 벗어난 변경이 있다면 이유: 없음.

## Game Studio route

- Umbrella: N/A — 운영사 runner/workflow 변경입니다.
- Specialist route: N/A
- 적용한 playfield/HUD/playtest 기준: runner가 직접 visible gameplay를 바꾸지 않습니다.
- Game Studio route에서 벗어난 변경이 있다면 이유: 게임 UI/asset/playfield를 수정하지 않았습니다. 단, runner가 만드는 다음 visible game WorkUnit은 Game Studio route와 Browser Use evidence를 요구합니다.

## 작업 checklist

- [x] Plan artifact의 수용 기준을 모두 확인했다.
- [x] 게임 기능/UI/에셋/QA 변경이면 Game Studio route를 기록했다. N/A — 운영사 runner 작업.
- [x] UI/HUD 변경이면 `game-studio:game-ui-frontend` 기준으로 playfield 보호와 persistent HUD 밀도를 확인했다. N/A.
- [x] 게임 화면 QA이면 `game-studio:game-playtest` 기준으로 첫 actionable screen, main verbs, HUD readability, playfield obstruction을 확인했다. N/A.
- [x] UI/visual 변경이면 Browser Use 우선 QA를 시도하고 evidence 또는 blocker를 남겼다. N/A — UI 변경 없음.
- [x] 필요한 문서/roadmap/dashboard/report를 갱신했다.
- [x] GitHub issue/PR/comment evidence를 축약 없이 남겼다.
- [x] all merge-blocking evidence must be in the original PR before merge/close.
- [x] post-merge main CI is observation-only; do not create a post-merge closeout PR or main-targeted closeout commit.

## 사용자/운영자 가치

- 게임 가치: queue empty가 checker-only 샛길이 아니라 `이상한 씨앗상회` production game quality WorkUnit intake로 이어집니다.
- 운영사 가치: 24h runner smoke/foreground/detached 실행법, heartbeat/report/watchdog 관찰법이 repo 안에 생겼습니다.

## Before / After 또는 Visual evidence

- Before: #276은 fixture checker였고 실제 runner 사용법이 없었습니다.
- After: `docs/STUDIO_HARNESS_V3_RUNNER_USAGE.md`, `scripts/studio-v3-live-runner.mjs`, `reports/operations/studio-v3-live-runner-20260503.md`
- Browser Use evidence 또는 blocker: N/A — docs/scripts-only runner change.
- N/A 사유: visible game UI 변경 없음.

## Playable mode

- main 실행 명령: `npm run play:main` 후 필요 시 `npm run play:main:install`, 그리고 `cd ../strange-seed-shop-play && npm run dev -- --host 127.0.0.1 --port 5174`
- 이 PR이 사람 플레이 환경을 막지 않는 이유: 런타임 게임 코드/asset/economy를 수정하지 않고 운영사 runner와 문서만 변경했습니다.

## 검증

- [x] `npm run studio:v3:runner -- --once --dry-run --issue 290 --item items/0147-studio-v3-24h-live-runner.md` PASS
- [x] `npm run check:studio-v3-live-runner` PASS
- [x] `npm run check:studio-v3-bot-runner` PASS
- [x] `npm run check:ops-live` PASS
- [x] `npm run check:ci` PASS
- [x] UI/visual 변경이면 Browser Use QA와 `npm run check:visual` 또는 명시 blocker + fallback PASS — N/A, UI 변경 없음.

## 안전 범위

- [x] 실제 결제, 로그인/account, ads SDK, 외부 배포, 고객 데이터, credential, 실채널 GTM 없음
- [x] `ENABLE_AGENT_AUTOMERGE` 변경 없음
- [x] Branch protection 우회 없음
- [x] Default runner mode는 `--dry-run`으로 GitHub mutation 없음

## 남은 위험

- 실제 24h unattended mutation은 credentials/tool/runtime이 있어야 한다. 이 PR은 runnable entrypoint와 dry-run smoke/usage를 제공하고, `--allow-create-issue --no-dry-run`을 명시 flag로 둔다.
- Runner가 실제 production gameplay issue를 구현하는 단계는 다음 WorkUnit이다.

## 연결된 issue

Closes #290
