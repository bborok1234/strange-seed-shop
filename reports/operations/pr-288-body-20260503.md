## 요약

#284/#285와 #286/#287이 GitHub 기준 CLOSED/MERGED/main CI green인데 `docs/ROADMAP.md`/control room mirror에 `review` active mission으로 남은 회귀를 #288 WorkUnit으로 분리해 정리했습니다. 닫힌 PR 뒤 closeout commit을 만들지 않고, 새 checker로 같은 stale mirror를 CI에서 실패시킵니다.

## Small win

- 이번 PR이 만든 가장 작은 승리: `npm run check:closed-workunit-mirrors`가 closed WorkUnit이 roadmap `review` 또는 control room active mission으로 남는 회귀를 잡습니다.

## Plan-first evidence

- Plan artifact: `items/0146-closed-workunit-mirror-consistency.md`
- Plan에서 벗어난 변경이 있다면 이유: 없음.

## Game Studio route

- Umbrella: N/A — 운영사 하네스/문서/체커 변경입니다.
- Specialist route: N/A
- 적용한 playfield/HUD/playtest 기준: N/A — visible gameplay 변경 없음.
- Game Studio route에서 벗어난 변경이 있다면 이유: 게임 UI/asset/playfield를 수정하지 않았습니다.

## 작업 checklist

- [x] Plan artifact의 수용 기준을 모두 확인했다.
- [x] 게임 기능/UI/에셋/QA 변경이면 Game Studio route를 기록했다. N/A — 운영사 하네스 작업.
- [x] UI/HUD 변경이면 `game-studio:game-ui-frontend` 기준으로 playfield 보호와 persistent HUD 밀도를 확인했다. N/A.
- [x] 게임 화면 QA이면 `game-studio:game-playtest` 기준으로 첫 actionable screen, main verbs, HUD readability, playfield obstruction을 확인했다. N/A.
- [x] UI/visual 변경이면 Browser Use 우선 QA를 시도하고 evidence 또는 blocker를 남겼다. N/A — UI 변경 없음.
- [x] 필요한 문서/roadmap/dashboard/report를 갱신했다.
- [x] GitHub issue/PR/comment evidence를 축약 없이 남겼다.
- [x] all merge-blocking evidence must be in the original PR before merge/close.
- [x] post-merge main CI is observation-only; do not create a post-merge closeout PR or main-targeted closeout commit.

## 사용자/운영자 가치

- 게임 가치: stale `review` mirror가 다음 production vertical slice intake를 가로막지 않게 합니다.
- 운영사 가치: GitHub-authoritative close state와 local evidence mirror가 어긋나는 회귀를 deterministic하게 잡습니다.

## Before / After 또는 Visual evidence

- Before: #284/#286이 GitHub에서는 닫혔지만 ROADMAP/control room에는 `review`/active mission으로 남았습니다.
- After: `reports/operations/closed-workunit-mirror-manifest-20260503.json`, `reports/operations/closed-workunit-mirror-report-20260503.md`
- Browser Use evidence 또는 blocker: N/A — docs/scripts-only harness change.
- N/A 사유: visible game UI 변경 없음.

## Playable mode

- main 실행 명령: `npm run play:main` 후 필요 시 `npm run play:main:install`, 그리고 `cd ../strange-seed-shop-play && npm run dev -- --host 127.0.0.1 --port 5174`
- 이 PR이 사람 플레이 환경을 막지 않는 이유: 런타임 게임 코드/asset/economy를 수정하지 않고 운영사 문서와 checker만 변경했습니다.

## 검증

- [x] `npm run check:seed-ops-queue` PASS
- [x] `npm run check:closed-workunit-mirrors` PASS
- [x] `npm run check:ops-live` PASS
- [x] `npm run check:dashboard` PASS
- [x] `npm run check:ci` PASS
- [x] UI/visual 변경이면 Browser Use QA와 `npm run check:visual` 또는 명시 blocker + fallback PASS — N/A, UI 변경 없음.

## 안전 범위

- [x] 실제 결제, 로그인/account, ads SDK, 외부 배포, 고객 데이터, credential, 실채널 GTM 없음
- [x] `ENABLE_AGENT_AUTOMERGE` 변경 없음
- [x] Branch protection 우회 없음
- [x] 닫힌 #284/#286 PR에 post-merge closeout commit을 만들지 않음

## 남은 위험

- 이 checker는 manifest에 기록된 closed WorkUnit mirror를 검증합니다. 새로운 closed WorkUnit을 자동으로 GitHub에서 수집하는 runner는 별도 slice가 필요합니다.
- GitHub API/CLI가 없는 CI 환경에서는 manifest가 최신이라는 전제만 검증합니다.

## 연결된 issue

Closes #288
