## 요약

정원 첫 화면에서 `달방울 누누` resident stage와 자동 생산/달빛 보호 주문을 따로 읽히는 카드 스택이 아니라 한 장면의 production engine으로 묶었습니다. `정원 생산 엔진 한 장면` playfield group을 노출하고, 달빛 보호 주문 상태에서 order crate/progress, 생산 수령, worker/roster 순서로 모바일 action surface를 압축했습니다.

## Small win

- 이번 PR이 만든 가장 작은 승리: 393×852 첫 정원 화면에서 `돌보기`, `생산 잎 수령`, `달빛 보호 주문 납품`이 하단 탭에 가리지 않고 같은 screen moment에서 보입니다.

## Plan-first evidence

- Plan artifact: `items/0144-first-screen-production-engine-layout.md`
- Plan에서 벗어난 변경이 있다면 이유: `src/App.tsx`에 `has-lunar-guardian-order` stage class를 추가했습니다. 처음에는 모든 creature stage production overlay에 적용했지만, full visual에서 달빛 harvest/복귀 성장 회귀가 발견되어 달빛 보호 주문 screen moment로 범위를 줄였습니다.

## Game Studio route

- Umbrella: `game-studio:game-studio`
- Specialist route: `game-studio:game-ui-frontend`, `game-studio:game-playtest`, `browser-use:browser`
- 적용한 playfield/HUD/playtest 기준: playfield 중심부 보호, persistent HUD edge tray 제한, 첫 actionable screen/main verbs/HUD readability/playfield obstruction screenshot evidence.
- Game Studio route에서 벗어난 변경이 있다면 이유: 없음.

## 작업 checklist

- [x] Plan artifact의 수용 기준을 모두 확인했다.
- [x] 게임 기능/UI/에셋/QA 변경이면 Game Studio route를 기록했다.
- [x] UI/HUD 변경이면 `game-studio:game-ui-frontend` 기준으로 playfield 보호와 persistent HUD 밀도를 확인했다.
- [x] 게임 화면 QA이면 `game-studio:game-playtest` 기준으로 첫 actionable screen, main verbs, HUD readability, playfield obstruction을 확인했다.
- [x] UI/visual 변경이면 Browser Use 우선 QA를 시도하고 evidence 또는 blocker를 남겼다.
- [x] 필요한 문서/roadmap/dashboard/report를 갱신했다.
- [x] GitHub issue/PR/comment evidence를 축약 없이 남겼다.
- [x] all merge-blocking evidence must be in the original PR before merge/close.
- [x] post-merge main CI is observation-only; do not create a post-merge closeout PR or main-targeted closeout commit.

## 사용자/운영자 가치

- 게임 가치: 첫 정원 화면이 “캐릭터 카드 + 생산 카드”가 아니라 누누가 일하고 주문 상자가 채워지는 idle production scene으로 읽힙니다.
- 운영사 가치: Browser Use current-session blocker, fallback screenshot, focused/full visual regression, `check:ci`까지 PR 전 evidence surface에 묶었습니다.

## Before / After 또는 Visual evidence

- Before: #275/#282 이후 resident/care는 복구됐지만 `.garden-stage.has-creature-stage .playfield-board-overlay`가 opacity 0이라 playfield production scene이 첫 화면에서 사라졌습니다.
- After: `reports/visual/first-screen-production-engine-one-scene-20260503.png`
- Browser Use evidence 또는 blocker: `reports/visual/browser-use-blocker-0284-20260503.md`
- Playtest report: `reports/visual/0284-first-screen-production-engine-playtest-20260503.md`

## Playable mode

- main 실행 명령: `npm run play:main` 후 필요 시 `npm run play:main:install`, 그리고 `cd ../strange-seed-shop-play && npm run dev -- --host 127.0.0.1 --port 5174`
- 이 PR이 사람 플레이 환경을 막지 않는 이유: save/economy 수치, manifest asset, runtime image generation, 배포 설정을 바꾸지 않고 모바일 layout/CSS 및 visual regression만 추가했습니다.

## 검증

- [x] `npm run check:visual -- --grep "생산 엔진|한 장면|production engine"` PASS — 1 passed
- [x] `npm run check:visual -- --grep "creature stage|기억 도장|생산 엔진|달빛 보호 주문 완료"` PASS — 4 passed
- [x] `npm run check:visual` PASS — 54 passed
- [x] `npm run check:ci` PASS
- [x] UI/visual 변경이면 Browser Use QA와 `npm run check:visual` 또는 명시 blocker + fallback PASS

## 안전 범위

- [x] 실제 결제, 로그인/account, ads SDK, 외부 배포, 고객 데이터, credential, 실채널 GTM 없음
- [x] `ENABLE_AGENT_AUTOMERGE` 변경 없음
- [x] Branch protection 우회 없음

## 남은 위험

- Browser Use `iab` 자체는 현재 세션에서 Node REPL `js` tool 미노출로 막혀 Playwright fallback screenshot을 사용했습니다.
- 달방울 누누 idle/care sprite strip은 후속 asset/FX WorkUnit에서 별도 provenance와 review가 필요합니다.

## 연결된 issue

Closes #284
